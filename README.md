**A "Hello World!" -style bootloader and toy kernel for the i386 architecture in both C and Rust.**

A detailed walkthrough at: http://kaspell.org/hello386

Usage:
* Build/run from C source: `make build` / `make run`
* Build/run from Rust source: `make build KERN_SRC=rust` / `make run KERN_SRC=rust`